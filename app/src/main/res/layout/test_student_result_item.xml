<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Название теста -->
    <TextView
        android:id="@+id/testName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:text="Тема 1. Работа с формами"
        android:textColor="@color/GrayForText"
        android:textStyle="bold" />

    <!-- Количество попыток -->
    <TextView
        android:id="@+id/attemptsCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:text="Процент верных оовтетов"
        android:textColor="@color/GrayForText"
        android:layout_marginTop="8dp" />

    <!-- Лучший результат -->
    <TextView
        android:id="@+id/bestScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:text="Оценка"
        android:textColor="@color/GrayForText"
        android:layout_marginTop="8dp" />

    <!-- Статистика (дополнительный текст) -->
    <TextView
        android:id="@+id/textViewbestMark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:text="Лучшая оценка"
        android:textColor="@color/GrayForText"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/statisticsLink"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:clickable="true"
        android:focusable="true"
        android:minHeight="48dp"
        android:onClick="onStatisticsClick"
        android:text="Статистика"
        android:textColor="@color/GrayForText"
        android:textSize="14sp"
        android:textStyle="bold|italic" />
</LinearLayout>